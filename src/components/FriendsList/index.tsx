import { type ReactElement } from 'react';

import GrayBox from '../ui/GrayBox';

export default function FriendsList(): ReactElement {
  return <GrayBox className='h-full w-[356px]'>FriendsList</GrayBox>;
}
